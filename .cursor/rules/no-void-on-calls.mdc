---
description: Do not use void on function calls
globs: "**/*.{ts,tsx}"
alwaysApply: false
---

# No void on function calls

Do not prefix function calls with `void`. It is unnecessary.

```typescript
onClick={() => signOut()}
```

Not:

```typescript
onClick={() => void signOut()}
```
